{
    current_build: Build
    uninstall: {
        safe[]: PackReference
        unsafe[]: UnsafeUninstall
    }
    # used primarily for '-/packs/uninstall/mark' and unmark tracking.
    # cleared every reload.
    # NO packs will be uninstalled unless ALL uninstall marks are safe.
    slimecore: {
        version: Version
        url: string
    }
}