// Called just after a <preload_entrypoint> ('#<preload_entrypoint.pack_ref>:preload_entrypoint/<preload_entrypoint.id>' tag) is called.

use ::slimecore::mcdoc::def::PackId
use ::slimecore::mcdoc::def::PreloadEntrypointId

struct Hook {
    pack_id: PackId,
    id: PreloadEntrypointId,
}